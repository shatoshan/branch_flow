# 2026-03-23 初期セットアップ

## 実施内容
- `BranchFlow/` を新規作成した
- `status/`, `backlog/`, `plan/`, `worklog/`, `materials/` を作成した
- `AGENTS.md` を追加した
- 初期 `status` と `plan` を作成した
- 初回 backlog として MVP 定義タスクを置いた

## 判断
- 予測装置としてではなく、条件監視と候補除外を中心にした端末として開始する
- 最初の active task は実装ではなく MVP 定義に置く
- worklog は日本語で継続する

## 次にやること
- MVP 範囲を 1 ページ化する
- 候補カード項目を 10 個以内に絞る
- ホーム画面の状態遷移を固定する
